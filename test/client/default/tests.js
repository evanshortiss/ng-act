var expect = chai.expect;
var assert = chai.assert;

describe('Test FH.Act', function() {

    it('Check angular and $fh is defined', function() {
        assert.ok(window.angular);
        assert.ok(window.$fh);
    });

});
